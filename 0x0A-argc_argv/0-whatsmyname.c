/**
 * main - print file name of program
 * @argc: unused arg count
 * @argv: arg array
 * Return: 0
 */

#include <stdio.h>

int main(int __attribute__((unused))argc, char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
